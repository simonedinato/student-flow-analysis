title: Student Flow Analysis
author: Applied Econometrics Team
logo: ""  # Add logo path if available
execute:
  execute_notebooks: off  # Skip execution during builds to avoid long runtimes

html:
  use_issues_button: false
  use_repository_button: false

sphinx:
  config:
    html_theme_options:
      secondary_sidebar_items: []
    html_js_files:
      - custom.js
